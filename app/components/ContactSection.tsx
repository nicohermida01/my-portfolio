import { pageSections } from 'ssot/pageSections'

export function ContactSection() {
	return (
		<section id={pageSections.CONTACT} className='w-full'>
			<div className='w-full max-w-appMW mx-auto px-8 py-16'>Contact</div>
		</section>
	)
}
