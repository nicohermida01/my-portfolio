import { pageSections } from 'ssot/pageSections'

export function SkillsSection() {
	return (
		<section id={pageSections.SKILLS} className='w-full'>
			<div className='w-full max-w-appMW mx-auto px-8 py-16'>Skills</div>
		</section>
	)
}
